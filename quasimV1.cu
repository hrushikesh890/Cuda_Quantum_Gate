#include <stdio.h>
#include <cuda_runtime.h>
#include <math.h>
#include <vector>
#include <string.h>

void fetch_data()

int main(int argc, const char *argv[]){
    float u[2][2];
    vector<float> vec;

    std::string filename = argv[1];
    printf("%s", filename);



}